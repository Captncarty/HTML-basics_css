//Map method is used to take each item in an array and pass it to a fucntion
const timeStamp = [10, 15, 30];
const newStamp = timeStamp.map((stamp) => stamp * 3);
console.log(timeStamp);
console.log(newStamp);
